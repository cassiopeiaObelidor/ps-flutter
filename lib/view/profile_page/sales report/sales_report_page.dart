import 'package:flutter/material.dart';

class SalesReportPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sales Report'),
      ),
      body: Center(
        child: Text('Sales Report Page Content'),
      ),
    );
  }
}
